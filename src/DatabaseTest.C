#include "Database.h"

using namespace std;

int main(){
    //Database::getInstance().mailing_list();
    //printf("Removing IP address: 10.0.0.1");
    //Database::getInstance().remove_from_blacklist('');
    //printf("IP address 10.0.0.1 has been removed");
    //printf(Database::getInstance().in_whitelist(''));
    return 0;
}